## scripts/ — прокси-цепочка РФ → зарубежный VPS

Скрипты поднимают цепочку, в которой ваш трафик выходит в интернет с иностранного IP. Зарубежный VPS — выходной узел; РФ-сервер (опционально) — всегда-онлайн relay/хаб перед ним.

```
[ПК / телефон] -> [РФ-сервер :1080] -> [зарубежный VPS :39847] -> Claude / интернет
```

### Назначение скриптов

| Скрипт | Где запускать | Что делает |
|---|---|---|
| [setup-foreign-vps.sh](setup-foreign-vps.sh) | зарубежный VPS (Ubuntu 24.04 / Debian 12–13), root | Поднимает **выходной** SOCKS5-узел на Dante с обязательной авторизацией логин/пароль. Наружу трафик уходит с иностранного IP VPS. |
| [setup-ru-relay.sh](setup-ru-relay.sh) | РФ-сервер (Ubuntu 24.04 / Debian 12–13), root | Поднимает **релей** на 3proxy: локально слушает SOCKS5 с авторизацией, весь трафик пробрасывает на зарубежный Dante (parent). Удобен при плавающем домашнем IP — клиент логинится по паролю. |
| [setup-foreign-wireguard.sh](setup-foreign-wireguard.sh) | зарубежный VPS (Ubuntu 24.04 / Debian 12–13), root | Бонус: полноценный **VPN** (WireGuard). Весь трафик телефона/ноутбука идёт через VPS. Выдаёт клиентский конфиг + QR. Альтернатива точечному SOCKS, особенно для телефона и приложений без поддержки прокси. |
| [_lib.sh](_lib.sh) | подключается всеми (НЕ запускается сам) | **Общая библиотека — единый источник правды.** Валидаторы (`valid_port`/`valid_ipv4`/`no_colon`…), детект SSH-портов и внешнего интерфейса, и анти-локаут: `ufw_is_active` (fail-closed гейт), `ufw_orchestrate` (на первом запуске разрешает SSH-порты и включает ufw ТОЛЬКО если хоть одно SSH-allow прошло; на re-run SSH не трогает), `ufw_clear_port` (перезапись правила порта, SSH-порты НИКОГДА не удаляет). **Кладите рядом с каждым setup-\*.sh.** |

> Также в наборе: [setup-ru-sshkey.sh](setup-ru-sshkey.sh) (ключ РФ→зарубеж), [setup-foreign-bastion.sh](setup-foreign-bastion.sh) (запереть SSH на IP РФ-сервера), [setup-foreign-mtproxy.sh](setup-foreign-mtproxy.sh) (личный MTProto для Telegram), и Windows-инструменты [proxy-tunnel.ps1](proxy-tunnel.ps1) / [make-proxifier-profile.ps1](make-proxifier-profile.ps1) — см. разделы «Доп. автоматизация» и про MTProto ниже.

### Порядок запуска

1. **Сначала** на зарубежном VPS — [setup-foreign-vps.sh](setup-foreign-vps.sh). Запишите выданные IP, порт (`39847`), логин (`proxyuser`), пароль.
2. **Затем** на РФ-сервере — [setup-ru-relay.sh](setup-ru-relay.sh), вписав данные из шага 1 в переменные `FOREIGN_*`.
3. На клиенте (Proxifier на Windows): SOCKS5 на `IP_РФ_сервера:1080`, логин/пароль `relayuser` / `LOCAL_PASS`.

WireGuard ([setup-foreign-wireguard.sh](setup-foreign-wireguard.sh)) — независимый путь: ставится на зарубежный VPS, клиент может цепляться к нему напрямую, РФ-релей для него не обязателен.

### Согласованность переменных и портов

Значения из выходного узла должны совпадать с тем, что вписано в релей:

| Параметр | setup-foreign-vps.sh | setup-ru-relay.sh | По умолчанию |
|---|---|---|---|
| IP зарубежного VPS | выводится в итоге | `FOREIGN_VPS_IP` | — (заменить!) |
| Порт SOCKS Dante | `SOCKS_PORT` | `FOREIGN_SOCKS_PORT` | `39847` |
| Логин Dante | `PROXY_USER` | `FOREIGN_USER` | `proxyuser` |
| Пароль Dante | `PROXY_PASS` (или автоген) | `FOREIGN_PASS` | — (заменить!) |
| Кому открыт SOCKS-порт | `ALLOW_FROM` = IP РФ-сервера | — | пусто = порт не открыт |
| Локальный порт релея | — | `LOCAL_PORT` | `1080` |
| Логин клиента (Proxifier) | — | `LOCAL_USER` | `relayuser` |
| Пароль клиента | — | `LOCAL_PASS` (или автоген) | пусто = автоген |
| Ограничить источники к релею | — | `ALLOW_FROM_CIDR` | пусто = отовсюду |

### Какие переменные править

**[setup-foreign-vps.sh](setup-foreign-vps.sh)** — передаются как переменные окружения при запуске:

| Переменная | Назначение |
|---|---|
| `ALLOW_FROM` | **Обязательно.** IP/CIDR РФ-сервера. Пусто → SOCKS-порт не откроется в ufw (безопасно по умолчанию). `0.0.0.0/0` открывает порт всему интернету — избегать. |
| `SOCKS_PORT` | Порт Dante (по умолчанию `39847`). |
| `PROXY_USER` | Логин SOCKS (по умолчанию `proxyuser`). |
| `PROXY_PASS` | Пароль SOCKS. Пусто → сгенерируется через openssl и покажется в конце. |
| `LISTEN_ADDR` | Адрес прослушивания (по умолчанию `0.0.0.0`; доступ всё равно держится на ufw `ALLOW_FROM`). |

**[setup-ru-relay.sh](setup-ru-relay.sh)** — правятся в блоке КОНФИГУРАЦИИ в начале файла:

| Переменная | Назначение |
|---|---|
| `FOREIGN_VPS_IP` | **Обязательно.** Публичный IP зарубежного VPS. |
| `FOREIGN_SOCKS_PORT` | Порт Dante (`39847`). |
| `FOREIGN_USER` | Логин Dante (`proxyuser`). |
| `FOREIGN_PASS` | **Обязательно.** Пароль Dante. |
| `LOCAL_PORT` | Порт релея для клиентов (`1080`). |
| `LOCAL_USER` | Логин клиента (`relayuser`). |
| `LOCAL_PASS` | Пароль клиента. Пусто → переиспользуется из существующего конфига или генерируется openssl. |
| `ALLOW_FROM_CIDR` | Опционально: ограничить, с каких IP/диапазонов принимать подключения к `LOCAL_PORT`. |
| `EXPECTED_DEB_SHA256` | Опционально: пин SHA256 доверенного .deb 3proxy (см. предупреждения). |

**[setup-foreign-wireguard.sh](setup-foreign-wireguard.sh)** — правятся в начале файла: `WG_PORT` (`51820/udp`), `WG_SUBNET` (`10.8.0.0/24`), `SERVER_WG_IP`, `CLIENT_WG_IP`, `CLIENT_DNS`.

### Как залить скрипт на сервер

> ⚠️ **Общая библиотека [_lib.sh](_lib.sh).** Все `setup-*.sh` подключают её (валидаторы, детект SSH-портов/интерфейса, ufw-хелперы вынесены туда — единый источник правды, без расхождений между копиями). Поэтому:
> - **копируйте `_lib.sh` В ТОТ ЖЕ каталог**, что и сам скрипт;
> - **запускайте скрипт файлом** (`sudo bash setup-*.sh`), **а НЕ через stdin/pipe** — при `ssh ... 'bash -s' < setup-*.sh` путь к `_lib.sh` не определить.
> Скрипт без `_lib.sh` рядом завершится с понятной ошибкой (а не молча).

Через `scp` (с вашей машины) — копируйте скрипт **вместе с `_lib.sh`**:
```sh
scp _lib.sh setup-foreign-vps.sh root@VPS_IP:/root/
scp _lib.sh setup-ru-relay.sh     user@RU_SERVER:/tmp/
```
Или вставьте содержимое **обоих** файлов в редактор прямо на сервере (`nano _lib.sh`, затем `nano setup-foreign-vps.sh`, Ctrl+O, Ctrl+X). `_lib.sh` — тоже `.sh`, ему нужны LF (см. ниже).

### ВАЖНО: окончания строк (LF)

Скрипты `.sh` должны иметь **Unix-окончания строк (LF)**, иначе bash выдаст ошибки вида `$'\r': command not found` или `bad interpreter`. Если редактировали файл в Windows (или копировали через буфер обмена), на сервере **перед запуском** выполните одно из:

```sh
sed -i 's/\r$//' setup-foreign-vps.sh
# или
dos2unix setup-foreign-vps.sh   # apt install dos2unix
```

Повторите для каждого `.sh`-скрипта, который трогали в Windows (в т.ч. для новых [setup-ru-sshkey.sh](setup-ru-sshkey.sh) и [setup-foreign-bastion.sh](setup-foreign-bastion.sh)). Скрипты `.ps1` запускаются на Windows и в конвертации LF не нуждаются.

### Запуск

Все `.sh`-скрипты запускаются от root:

```sh
sudo bash setup-foreign-vps.sh        # с ALLOW_FROM=... перед командой
sudo bash setup-ru-relay.sh
sudo bash setup-foreign-wireguard.sh
```

Скрипты идемпотентны — повторный запуск безопасен (пакеты доустанавливаются по необходимости, конфиги переписываются, правила ufw не дублируются, существующие пароли/ключи не перетираются без нужды).

---

## Доп. автоматизация

Вспомогательные артефакты, которые избавляют от ручных шагов инструкции: генерация профиля Proxifier, держание SSH-туннеля живым, подготовка ключа и бастионное запирание SSH. Два из них (`.ps1`) запускаются **на Windows-ПК**, два (`.sh`) — **на серверах**.

| Скрипт | Где запускать | Что делает |
|---|---|---|
| [make-proxifier-profile.ps1](make-proxifier-profile.ps1) | Windows-ПК | Генерирует импортируемый профиль Proxifier (`.ppx`): один SOCKS5-прокси (РФ-релей или зарубежный VPS), правило «AI» заворачивает указанные приложения через прокси, «Default» — напрямую, DNS резолвится через прокси (без DNS-утечки). Заменяет ручную настройку раздела про Proxifier. |
| [proxy-tunnel.ps1](proxy-tunnel.ps1) | Windows-ПК | Поднимает локальный SOCKS5 через **прямой** SSH-туннель (`ssh -D`) к зарубежному VPS и держит его живым (авто-переподключение с экспоненциальной паузой). Может ставиться как Scheduled Task (автозапуск при логоне). Альтернатива связке РФ-релей (3proxy) + Dante. |
| [setup-ru-sshkey.sh](setup-ru-sshkey.sh) | РФ-сервер, root (или будущий пользователь autossh) | Готовит беспарольный (key-based) SSH-доступ **с РФ-сервера** на зарубежный VPS: генерирует ed25519-ключ, копирует публичную часть на VPS (`ssh-copy-id` или ручной фолбэк), проверяет вход по ключу, печатает egress-IP РФ-сервера. Фундамент для бастиона и autossh. |
| [setup-foreign-bastion.sh](setup-foreign-bastion.sh) | зарубежный VPS, root | Запирает SSH этого VPS так, чтобы зайти по SSH можно было **только** с публичного IP РФ-сервера. Анти-локаутный порядок: валидация IP → определение реальных SSH-портов → узкие `allow from` → подтверждение → удаление широких правил (вкл. парные IPv6). Правило SOCKS-порта не трогает. |

### Параметры

**[make-proxifier-profile.ps1](make-proxifier-profile.ps1)** — параметры командной строки:

| Параметр | Назначение | По умолчанию |
|---|---|---|
| `-ProxyHost` | **Обязательно.** IP/хост первого звена: РФ-релея (`IP_РФ_сервера`) либо напрямую зарубежного VPS. | — |
| `-ProxyPort` | TCP-порт SOCKS5 на `-ProxyHost`. | `1080` (порт 3proxy-релея) |
| `-ProxyUser` | Логин SOCKS5. Пароль в файл **не пишется** — введёте в GUI один раз. | `relayuser` |
| `-Apps` | Приложения, чей трафик заворачивается в прокси (через `;`). | `Claude.exe;Antigravity.exe;chrome.exe` |
| `-OutFile` | Путь к выходному `.ppx`. | `.\proxy-ai.ppx` |
| `-Force` | Перезаписать существующий `-OutFile` без предупреждения. | — |

**[proxy-tunnel.ps1](proxy-tunnel.ps1)** — параметры командной строки:

| Параметр | Назначение | По умолчанию |
|---|---|---|
| `-VpsIp` | **Обязательно.** IP/хостнейм зарубежного VPS. | — |
| `-VpsUser` | Пользователь SSH на VPS. | `root` |
| `-LocalPort` | Локальный порт SOCKS5 (`127.0.0.1:LocalPort`). | `1080` |
| `-SshPort` | Порт SSH на VPS. | `22` |
| `-Install` | Зарегистрировать как Scheduled Task (автозапуск при логоне). | — |
| `-Uninstall` | Удалить Scheduled Task (`-LocalPort` должен совпадать с установкой — он входит в имя задачи). | — |

**[setup-ru-sshkey.sh](setup-ru-sshkey.sh)** — правятся в блоке КОНФИГУРАЦИИ в начале файла:

| Переменная | Назначение | По умолчанию |
|---|---|---|
| `FOREIGN_VPS_IP` | **Обязательно.** Публичный IP зарубежного VPS. | — (заменить!) |
| `FOREIGN_SSH_PORT` | SSH-порт зарубежного VPS. | `22` |
| `FOREIGN_SSH_USER` | Пользователь на VPS, которому даём доступ по ключу. | `root` |
| `KEY_PATH` | Путь к приватному ключу (публичный — `+.pub`). | `~/.ssh/id_ed25519` |
| `KEY_PASSPHRASE` | Пусто = headless-ключ без passphrase (нужно для autossh/бастиона). Заполнять только если headless не нужен и используете ssh-agent. | пусто |

**[setup-foreign-bastion.sh](setup-foreign-bastion.sh)** — через переменные окружения при запуске:

| Переменная | Назначение | По умолчанию |
|---|---|---|
| `RU_SERVER_IP` | **Обязательно.** Публичный IPv4 РФ-сервера (бастиона) — единственный источник, которому будет разрешён SSH к VPS. | — |
| `CONFIRM` | `yes` — подтвердить удаление широких SSH-правил неинтерактивно. Иначе скрипт спросит в терминале (без TTY и без `CONFIRM=yes` широкие правила НЕ удаляются — безопасный отказ). | пусто |

### Как разрешить запуск .ps1 на Windows

PowerShell по умолчанию может блокировать запуск скриптов. Снимите ограничение **только для текущей сессии** (политику системы не меняет):

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

Если файл скачан/получен через буфер и помечен как «из интернета», снимите блокировку зоны (Mark-of-the-Web):

```powershell
Unblock-File .\make-proxifier-profile.ps1
Unblock-File .\proxy-tunnel.ps1
```

`proxy-tunnel.ps1` при установке как Scheduled Task сам запускает себя с `-ExecutionPolicy Bypass` для `-File` (только для этого файла, глобальную политику не трогает).

### Порядок применения

1. **Профиль Proxifier** (опционально, вместо ручной настройки): на Windows-ПК
   ```powershell
   .\make-proxifier-profile.ps1 -ProxyHost <IP_РФ_сервера>
   ```
   затем в Proxifier: `Profile → Import Profile…`, выбрать `proxy-ai.ppx`, ввести пароль прокси один раз. При ошибке импорта (`Cannot import / Unsupported profile`) — настройте прокси вручную теми же `Host/Port/User`, профиль используйте как шпаргалку.

2. **Прямой SSH-туннель** (опционально, альтернатива связке РФ-релей + Dante): на Windows-ПК
   ```powershell
   .\proxy-tunnel.ps1 -VpsIp <IP_зарубежного_VPS> -VpsUser root -LocalPort 1080
   ```
   Первый запуск делать **вручную в консоли** (foreground) — чтобы принять host key и/или ввести пароль. Затем при желании `-Install` для автозапуска. В Proxifier/браузере: `127.0.0.1:1080`, SOCKS5, без логина/пароля.

3. **Бастион (закрытие SSH зарубежного VPS на IP РФ-сервера)** — **строго в таком порядке**, иначе риск локаута:
   1. На **РФ-сервере** запустите [setup-ru-sshkey.sh](setup-ru-sshkey.sh) (в интерактивной сессии — нужен TTY для разового ввода пароля VPS). Из итоговой сводки запишите **публичный egress-IP РФ-сервера**.
   2. Убедитесь, что вход по ключу **с РФ-сервера на VPS реально работает** (скрипт это проверяет: строка `OK: вход по ключу … работает без пароля`).
   3. **ТОЛЬКО ПОТОМ** на **зарубежном VPS** запустите [setup-foreign-bastion.sh](setup-foreign-bastion.sh), передав egress-IP РФ-сервера:
      ```sh
      RU_SERVER_IP=<egress-IP_РФ-сервера> bash setup-foreign-bastion.sh
      ```
      Перед этим **обязательно** откройте аварийную консоль провайдера (KVM/serial/VNC/recovery) — это единственная страховка от локаута.

### Сводка предупреждений по безопасности (доп. автоматизация)

**Бастион — риск локаута (самый серьёзный).** При неверном/изменившемся `RU_SERVER_IP` (опечатка, не тот IP, динамический/CGNAT-адрес РФ-сервера) вы **заблокируете себе SSH** к VPS. Единственное спасение — **аварийная консоль провайдера** (`ufw allow OpenSSH`). Поэтому: запускайте бастион **только на зарубежном VPS** (у него обычно есть консоль; у РФ-сервера её может не быть), заранее откройте консоль в браузере и в **отдельной сессии** проверьте, что SSH к VPS проходит именно с РФ-сервера. Скрипт сначала добавляет узкие `allow from`, громко предупреждает и требует подтверждения, и лишь затем удаляет широкие правила. Ручной откат одной строкой через консоль провайдера: `ufw allow OpenSSH` (и при нестандартном порту `ufw allow <порт>/tcp`).

**Порядок бастиона.** Сначала [setup-ru-sshkey.sh](setup-ru-sshkey.sh) на РФ, убедиться что вход по ключу работает, **ТОЛЬКО потом** [setup-foreign-bastion.sh](setup-foreign-bastion.sh) на зарубежном. Иначе после запирания SSH вы не сможете зайти на VPS.

**SSH-ключ без passphrase.** [setup-ru-sshkey.sh](setup-ru-sshkey.sh) намеренно создаёт ключ **без passphrase** ради headless-автоматизации (autossh/бастион переподнимаются без человека). Платой является то, что приватный ключ — «носитель доступа как есть»: кто прочитает файл, тот войдёт на VPS. Скрипт держит права `600` на приватный ключ; на зарубежной стороне дополнительно ограничьте доступ (`from="egress-IP"` в `authorized_keys`, отдельный непривилегированный пользователь вместо root, firewall по IP). Если headless не нужен — задайте `KEY_PASSPHRASE` и используйте ssh-agent.

**Профиль Proxifier (.ppx) — пароль вводится в GUI.** В `.ppx` записывается только логин; пароль прокси **не пишется в файл** и вводится один раз в GUI Proxifier (он хранит его в собственном зашифрованном хранилище). Файл содержит публичный IP и логин прокси — не публикуйте `.ppx` в открытых репозиториях/чатах, добавьте `*.ppx` в `.gitignore`. После импорта проверьте в GUI, что включена опция «Resolve hostnames through proxy» (защита от DNS-утечки). Формат `.ppx` версионно-зависим — при отказе импорта настройте прокси вручную.

**Прямой SSH-туннель.** Локальный SOCKS5 биндится только на `127.0.0.1` (не виден из сети) — не меняйте на `0.0.0.0`. Для автозапуска (скрытая Scheduled Task) нужен вход по ключу: при первом запуске сделайте его **вручную** в консоли, чтобы принять host key (сверьте fingerprint с тем, что показывал сервер) и/или ввести пароль — иначе скрытая задача зависнет в ожидании пароля. Параметры `VpsIp/VpsUser` видны в экспорте задачи Task Scheduler (это не секреты, но раскрывают топологию).

**Секреты в файлах.** Приватный SSH-ключ — `600` у владельца, `~/.ssh` — `700`, `authorized_keys` на зарубежной стороне — `600`. Не коммитьте заполненные реальными значениями скрипты/профили в публичный репозиторий — `FOREIGN_VPS_IP`/порт/пользователь и egress-IP раскрывают топологию доступа.

**Эксплуатационный/юридический риск.** Проксирование трафика из РФ через зарубежный VPS может нарушать ToS провайдера и местное законодательство — ответственность на пользователе; скрипты лишь технически настраивают софт.

---

### Сводка предупреждений по безопасности (базовая цепочка)

**Защита от SSH-локаута.** Все три базовых скрипта разрешают SSH в ufw (профиль OpenSSH + реальный текущий SSH-порт, в т.ч. при socket-активации `ssh.socket` на Ubuntu 22.10+/24.04) **до** включения файрвола. Если запускаете не через SSH (консоль VPS-панели) и порт нестандартный — заранее разрешите его вручную: `ufw allow <порт>/tcp`. При первом запуске на удалённом сервере держите **вторую SSH-сессию** открытой.

**Облачный файрвол провайдера.** ufw открывает порты только на самом сервере. Если у VPS есть security group в панели хостинга — нужный порт (`39847/tcp` для Dante, `51820/udp` для WireGuard) откройте там отдельно.

**Доступ к выходному узлу.** В [setup-foreign-vps.sh](setup-foreign-vps.sh) по умолчанию (`ALLOW_FROM` пуст) SOCKS-порт вообще не открыт — это намеренно. Открывайте его **только** для IP РФ-сервера. `ALLOW_FROM=0.0.0.0/0` оставляет единственным барьером пароль — избегайте.

**Пароли.** SOCKS5 защищён только паролем учётки и передаёт его практически открытым текстом. Используйте длинный случайный пароль (автоген через openssl) и храните в менеджере паролей — этот пароль = доступ к вашему иностранному IP. По возможности задавайте `ALLOW_FROM` / `ALLOW_FROM_CIDR`, чтобы ограничить источники.

**Секреты в файлах.** Пароли хранятся в открытом виде в `/etc/danted.conf` (600) и `/etc/3proxy/3proxy.cfg` (600, владелец — сервисный пользователь); ключи WireGuard — в `/etc/wireguard/` (600). Любой root на сервере их увидит. **Не коммитьте заполненные скрипты/конфиги в git.** Сгенерированные пароли видны в итоговом выводе скрипта — не оставляйте их в скроллбэке терминала/логах CI/чатах, очистите экран после сохранения.

**brute-force.** На зарубежном VPS поднимается fail2ban-jail для Dante (формат лог-строк Dante 1.4.x — после запуска проверьте `fail2ban-regex /var/log/danted.log /etc/fail2ban/filter.d/danted.conf`). У РФ-релея anti-bruteforce на SOCKS-порту нет — при публично открытом `LOCAL_PORT` используйте `ALLOW_FROM_CIDR`, длинный пароль или вынос за VPN.

**Подлинность 3proxy .deb.** Апстрим 3proxy не публикует подписей/контрольных сумм. По умолчанию [setup-ru-relay.sh](setup-ru-relay.sh) делает только integrity-проверку (валидный .deb, не 404-HTML); подлинность криптографически не подтверждается. Для доверенной установки впишите хэш в `EXPECTED_DEB_SHA256`, полученный из независимого источника. При недоступности .deb скрипт собирает 3proxy из исходников по тегу релиза `3proxy-0.9.6` (откат к master помечается в логах).

**WireGuard.** Клиентский конфиг содержит приватный ключ и печатается в терминал (текст + QR) — передавайте только по защищённому каналу, очищайте буфер/историю. `DEFAULT_FORWARD_POLICY` переключается в `ACCEPT` (нужно для маршрутизации) и остаётся таким после удаления VPN — при демонтаже верните в `DROP`. Проверьте строку `Endpoint`: за провайдерским NAT туда может попасть внутренний адрес — впишите реальный публичный IP вручную.

**Эксплуатационный/юридический риск.** Проксирование трафика из РФ через зарубежный VPS может нарушать ToS провайдера и местное законодательство — ответственность на пользователе; скрипты лишь технически настраивают софт.


---

## setup-foreign-mtproxy.sh — личный MTProto-прокси для Telegram (Fake TLS)

**Где запускать:** на **зарубежном** VPS, от root. **Только для Telegram** (не для Chrome/Claude — там SOCKS5).

Личный MTProto-прокси на базе [mtg](https://github.com/9seconds/mtg) с Fake TLS: не попадает в публичные списки (РКН не косит по кластерам), скорость только твоя, маскируется под HTTPS реального домена.

```sh
sudo bash setup-foreign-mtproxy.sh
```

**Переменные (необязательно):**

| Переменная | По умолчанию | Назначение |
|---|---|---|
| `MTPROXY_PORT` | `443` | порт прокси; 443 = максимальная маскировка под HTTPS. **Должен быть свободен** (нет nginx/apache на 443) |
| `FAKE_TLS_DOMAIN` | `www.cloudflare.com` | домен для маскировки TLS-handshake (реальный, с TLS 1.3) |
| `MTG_USER` | `mtg` | системный пользователь службы |

**Что выдаёт:** готовые ссылки `tg://proxy?...` и `https://t.me/proxy?...` — открой на телефоне/в Telegram Desktop, подтверди подключение.

**Важно:**
- Секрет генерируется **один раз** и хранится в `/etc/mtg/` — повторный запуск **не** ломает уже розданную ссылку.
- Порт `443` должен быть свободен; иначе задай `MTPROXY_PORT=8443`.
- Зависит от доступности GitHub API (скачивание mtg) и того, что выбранный `FAKE_TLS_DOMAIN` не заблокирован.
